


const refreshCars = (carCardHTML) => {
 const carsList = document.querySelector('.cars');
  carsList.insertAdjacentHTML('beforeend', carCardHTML);
  console.log("j'insere ma gueule");
};

refreshCars('<%= j render "cars/car_dashboard_card", car: @car %>');
